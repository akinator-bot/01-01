# 移动端股票分析应用依赖包
# 专为Android APK构建优化

# GUI框架
kivy>=2.1.0

# 数据处理核心库
pandas>=1.3.0
numpy>=1.21.0

# 股票数据获取
baostock>=0.8.9
# 注意：akshare在Android上可能有兼容性问题，优先使用baostock

# 网络请求
requests>=2.25.0
urllib3>=1.26.0
certifi>=2021.5.25
charset-normalizer>=2.0.0
idna>=3.2

# 日期时间处理
python-dateutil>=2.8.0
pytz>=2021.1

# 基础工具
six>=1.16.0

# 可选：轻量级数据可视化（如果需要图表功能）
# matplotlib-base>=3.3.0  # 注意：完整的matplotlib在Android上可能较大

# 可选：数据导出（如果需要）
# openpyxl>=3.0.0  # Excel支持，但会增加APK大小

# 系统兼容性
typing-extensions>=4.0.0

# 注意事项：
# 1. 避免使用过大的依赖库以减小APK体积
# 2. 某些库在Android上可能需要特殊配置
# 3. 优先选择纯Python实现的库
# 4. 测试时注意检查所有依赖是否正常工作