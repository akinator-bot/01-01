# 移动端股票分析应用依赖
# 这些依赖将由buildozer在构建Android APK时自动安装

# 核心框架
kivy>=2.1.0

# 网络请求
requests>=2.25.0

# 数据获取（可选，用于真实数据）
baostock>=0.8.8

# 注意：不再包含以下重型依赖，因为它们在Android构建中会导致问题：
# - pandas (已移除，使用Python标准库替代)
# - numpy (已移除，使用Python标准库替代)
# - matplotlib (移动端不需要复杂图表)
# - seaborn (移动端不需要)
# - openpyxl (移动端不需要Excel导出)
# - xlsxwriter (移动端不需要Excel导出)
# - akshare (依赖过多，移动端使用简化数据源)

# 所有数据处理现在使用Python标准库：
# - random (随机数生成)
# - statistics (统计计算)
# - datetime (时间处理)
# - json (数据序列化)
# - urllib (网络请求，requests的轻量替代)